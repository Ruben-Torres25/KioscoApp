<UserControl x:Class="KioscoApp.Views.ProductosView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KioscoApp.Views" xmlns:vm="clr-namespace:KioscoApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Código de Barras:" Margin="0,0,10,5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtCodigoBarras" Grid.Row="0" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,0,0,5"
                 Text="{Binding CodigoBarras, Mode=TwoWay}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nombre Producto:" Margin="0,0,10,5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtNombre" Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,0,0,5"
                 Text="{Binding Nombre, Mode=TwoWay}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Precio Venta:" Margin="0,0,10,5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPrecioVenta" Grid.Row="2" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,0,0,5"
                 Text="{Binding PrecioVenta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Guardar Producto" Margin="0,0,10,0"
                    Command="{Binding GuardarProductoCommand}"/>
            <Button Content="Editar Producto" Margin="0,0,10,0"
                    Command="{Binding EditarProductoCommand}"/>
            <Button Content="Limpiar Campos" Margin="0,0,10,0"
                    Command="{Binding LimpiarCamposCommand}"/>
            <Button Content="Eliminar Producto"
                    Command="{Binding EliminarProductoCommand}"
                    CommandParameter="{Binding SelectedProducto}"/>
        </StackPanel>

        <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" BorderThickness="0,0,0,1" BorderBrush="LightGray" Margin="0,0,0,10"/>

        <DataGrid x:Name="dgProductos" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                  AutoGenerateColumns="True" IsReadOnly="True"
                  SelectionMode="Single"
                  ItemsSource="{Binding Productos}"
                  SelectedItem="{Binding SelectedProducto, Mode=TwoWay}"/>
    </Grid>
</UserControl>